dashboard:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.dashboard
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=300
  volumes:
    - dashboard_data:/var/lib/data
  restart: unless-stopped
